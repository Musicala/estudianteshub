{
  "name": "estudiantes-hub-rules-test",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "cd ../.. && firebase emulators:exec --only firestore --project bitacoras-de-clase --config firebase.test.json \"node tests/firestore-rules/test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
